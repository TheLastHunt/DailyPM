services:
  - type: web
    name: daily-concept-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python daily_concept_app.py
    autoDeploy: true
